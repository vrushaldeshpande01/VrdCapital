apiVersion: apps/v1
kind: Deployment
metadata:
  name: report-service
  namespace: vrdcapital
  labels:
    app: report-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: report-service
  template:
    metadata:
      labels:
        app: report-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8007"
    spec:
      serviceAccountName: vrdcapital-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: report-service
          image: ACCOUNT_ID.dkr.ecr.ap-south-1.amazonaws.com/vrdcapital/report-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8007
              name: http
          envFrom:
            - configMapRef:
                name: vrdcapital-config
            - secretRef:
                name: vrdcapital-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8007
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /live
              port: 8007
            initialDelaySeconds: 20
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: vrdcapital
spec:
  selector:
    app: report-service
  ports:
    - port: 8007
      targetPort: 8007
      name: http
  type: ClusterIP
